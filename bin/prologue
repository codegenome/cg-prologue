#!/usr/bin/env ruby
# -*- mode: ruby -*-

# Requires
begin
  require 'prologue'
rescue LoadError
  require 'rubygems'
  require 'prologue'
end

# Setup Constants
::PROLOGUE_GEM_ROOT = File.expand_path( File.dirname( __FILE__ ) + '/../' )

# Run
Prologue::CLI.start

